import React from 'react';

const Dashboard = () => (
  <div style={{ maxWidth: 600, margin: 'auto', textAlign: 'center' }}>
    <h1>Dashboard</h1>
    <p>Welcome back! Go to the Candidates page to cast your vote.</p>
  </div>
);

export default Dashboard;
